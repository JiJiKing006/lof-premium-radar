# 项目长期约束

以下规则是本项目的长期硬约束。除非用户明确提到并要求修改，否则后续不得修改、弱化或绕开这些规则。

## 接口冻结规则

- 当前线上接口及其稳定性、性能优化结果视为冻结基线。
- 除非用户明确要求“修改接口”，后续任何任务（包括 UI、交互、样式、页面重构和小程序适配）都不得修改接口实现、服务端业务逻辑、接口参数与返回结构、缓存策略、数据源策略、部署配置或线上接口服务。
- UI 任务只能消费现有接口，不得以页面开发、联调、性能优化或兼容性处理为由顺带调整接口。
- 允许对接口进行只读检查和验证；若发现问题，只报告，不修改、不部署，等待用户明确授权。

## 接口环境与部署隔离

- 小程序开发阶段必须使用 `devApiBaseUrl` 指向测试接口；开发者工具、develop 和 trial 等非 release 环境不得请求线上接口。
- 小程序 release 正式版必须自动使用 `apiBaseUrl` 指向线上接口，不得携带测试接口地址。
- 开发阶段每次修改接口后，默认只允许部署测试服务并验证测试接口；测试部署命令为 `npm run deploy:test`。
- 只有用户在当前任务中明确提出“部署线上接口”时，才允许执行生产部署；不得因为测试完成、代码合并、页面发布或用户笼统说“部署”而自行部署线上接口。
- 测试与生产必须使用独立的进程、端口、发布目录、缓存/快照、访问统计和订阅文件；测试服务不得触发正式微信定时推送，也不得通过启动预热抢占资源影响线上服务。
- 当前约定：测试入口为 `https://jijiking.top/dev/api/...`，线上入口为 `https://jijiking.top/api/...`；接口业务路径和返回结构保持一致。

## 接口性能

- 首页、详情页以及后续新增的任何业务接口，都要优先保障首屏体验。
- 用户进入页面后，最迟 1.5 秒内应展示可用数据。
- 首页列表接口及从首页可见基金进入的详情接口，正常缓存与稳定快照条件下单次响应都不得超过 1.5 秒；测试部署必须同时执行首页和详情的 1.5 秒门禁。
- 接口优化目标是快且稳定，不能为了页面效果牺牲真实数据质量。
- 如果某个具体字段处理很慢，且短期无法优化，应先返回已经真实可用的数据，让页面先展示；慢字段后续补齐或明确显示 `暂无数据` / `净值未公布`。

## 接口稳定性

- 同一个接口在相同筛选条件下，返回规模必须稳定，不能出现“一会儿 8 条、一会儿 200 多条”的混乱情况。
- 基金分类、筛选参数、分页、排序和详情查询必须保持一致，不能因为前端携带错误分类导致基金错乱或详情缺失。
- 首页和详情页都必须能稳定返回可展示数据；新增接口也遵守同样要求。
- 任何接口的数据规模和已验证字段都必须稳定；T+3 申购状态出现成片缺失按 T0 级数据事故优先处理。

## 刷新字段完整性

- 页面刷新、手动下拉刷新、后台自动更新和分页追加都不得让已经展示的真实有效字段退化为空。
- 申购状态一旦从真实来源成功返回，后续刷新若补充来源超时、失败或只返回 `未知` / `暂无数据`，必须保留上一份真实有效状态及其来源和更新时间，并明确标记为沿用状态；不得用空值覆盖。
- 新快照返回了更新且真实可追溯的申购状态时，才允许替换旧状态。

## 小程序表格交互

- 首页观察表格横向滑动时，“自选”列必须始终固定冻结，不得跟随其他数据列移出视口。
- 冻结列不得遮挡相邻单元格，表头与表体必须对齐，纵向滚动和点击自选功能必须保持正常。
- 冻结列不得依赖滚动事件逐帧追位，必须避免左右滑动时的果冻感、抖动或短暂偏移。
- 申购限额不得用省略号隐藏；统一显示为“限 X 元”或“限 X 万”，超过一万元使用万元单位，但不得改变真实限额数值。
- 表头排序、暂停申购过滤和分页加载必须有即时可见反馈，且不得因重新触发整表聚合而表现为无响应。
- 首页触底分页属于 T0 稳定性能力：必须同时保留触底事件、接近底部判断和可点击加载入口，不得只依赖单一滚动事件；自动触底不得使用点击节流阻断，只能使用请求中的并发锁防重。
- 首页分页必须绑定首次列表返回的同一服务端快照；刷新或后台更新不得导致后续页重复、漏项、顺序漂移或无法继续加载。服务重启导致快照失效时，必须自动回到新快照第一页后继续加载，禁止把两份快照直接拼接。
- 分页失败必须自动重试并释放加载锁；任何成功、失败、超时和异常路径都不得让后续触底永久失效。

## 小程序筛选弹层宽度稳定性

- 首页筛选弹层必须始终限制在页面内容区内，使用明确的 `width: 100%`、`max-width: 100%` 和 `box-sizing: border-box`，任何机型都不得横向溢出、异常变宽或把内容裁出屏幕。
- 筛选项、输入框和底部按钮必须设置可收缩宽度与 `min-width: 0`，不得依赖最长文案或剩余空间撑开弹层；修改筛选 UI 后必须在微信开发者工具常见手机尺寸下检查左右边界。
- 筛选入口角标必须紧贴“筛选”文案并按内容自适应，不得使用 `margin-left: auto` 占据按钮剩余宽度；筛选入口本身必须使用明确且稳定的宽度。

## 首页与详情接口 T0 稳定性

- 首页默认按实时溢价率倒序，并以基金代码作为相同排序值的稳定次序；同一快照内重复请求不得改变基金顺序。
- 首页基金范围只展示 `LOF`，包括真实归类为 LOF 的 QDII-LOF；暂不展示 ETF、独立 QDII ETF 以及申购状态为“场内交易”或 `exchange` 的纯场内基金。
- 首页溢价表只允许展示同时具备真实现价、已公布官方净值和可计算实时溢价率的完整数据行；任一关键字段缺失时必须隐藏该行，禁止用估算净值、空值、零值或伪造值补齐。
- 首页 Tab 每次切换都必须重新获取当前栏目数据；进入自选栏目之前必须重新读取本地自选代码，确保刚加入的基金立即出现。
- 搜索结果允许真实的空数组；不得把“无匹配结果”当成接口异常并回退显示未搜索前的缓存数据。
- 从首页可见列表进入的合法基金详情不得返回 404。聚合快照偶发缺行时必须查询真实单基金来源或读取可追溯的最后有效快照；数据源暂不可用应返回 503，不得误报基金不存在。
- 同一服务端快照中的详情现价、今日估算净值和实时溢价率必须与首页对应基金同步；刷新失败不得让详情中已经展示的真实估值或实时溢价率退化为空。
- 详情必须保留 `estimatedNav` 今日估值字段；场内市值只允许按“真实场内现价 × 交易所披露场内份额”计算，并同时返回份额来源、份额日期和计算口径。缺少任一真实因子时显示 `暂无数据`，不得把基金资产净值或第三方份额冒充场内市值。
- 首页有效基金规模属于 T0 稳定性指标；新快照的完整可展示行数异常下降时不得替换上一份有效快照，必须保留上一份真实快照并标记沿用，直至新快照恢复或确认基金范围真实变化。
- 估值有效日必须绑定场内价格所属交易日，不得在自然日零点仅因日期切换就让上一交易日价格与同交易日真实估值整批失效；在新交易日场内行情出现前，继续沿用最后交易日可追溯快照并显示原始价格、估值时间及沿用状态。新交易日价格出现后，必须要求同日或更新的真实估值，禁止把上一交易日估值与新交易日价格混算。

## 刷新交互

- 页面下拉刷新或其他明确的用户主动刷新在请求期间，必须显示“加载数据中” loading。
- loading 必须设置 `mask: true`，请求结束、失败或异常时都必须在 `finally` 中关闭，防止重复操作和遮罩残留。
- 分页上拉追加数据不得弹出全局 loading 或遮罩，不得阻断用户阅读已展示数据。

## 数据真实性

- 数据必须真实、实时、可追溯。
- 不得使用模拟数据、随机数据、伪造行情、硬编码行情、手动推断净值或手动推断溢价率。
- 实时价格、官方净值、溢价率、成交额、成交量、份额等金融字段必须来自真实来源，并保留来源与精确到秒的更新时间。
- 官方净值缺失时显示 `净值未公布`；其他缺失值显示 `暂无数据`。
- 估算净值只能作为独立字段展示，不能替代官方净值，也不能用于冒充官方溢价计算。
- “实时溢价率”必须使用真实场内现价与今日可追溯估算净值计算：`(现价 - 今日估算净值) / 今日估算净值`；不得使用昨日或更早官方净值冒充实时口径。
- 官方净值口径溢价率必须保留为独立字段，继续只使用已公布官方净值计算；实时估算口径与官方口径不得混用、互相覆盖或使用相同标签误导用户。
- 首页实时溢价榜中的每一行都必须具备今日估算净值、估值来源和精确到秒的估值时间；多来源均无今日估值时隐藏该行，详情显示 `暂无数据`，不得用官方净值回填实时溢价率。

## 优先级

```text
真实 > 稳定 > 首屏速度 > 完整度 > 页面表现
```

当速度和完整度冲突时，先返回真实且稳定的核心数据；当速度和真实性冲突时，必须选择真实性。
