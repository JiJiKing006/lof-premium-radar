# 待开发需求清单

用法：你只需要在对应优先级下面按有序列表写一句简单需求。状态用 `[待开发]`、`[开发中]`、`[测试中]`、`[已开发]` 区分。运行 `npm run workflow:prompt` 后，脚本会根据项目结构、数据真实性规则，把这句需求扩展成可交给 Codex 执行的完整提示。

## P0（立即开发）

1. [已开发] 新增加纳斯达克科技数据来源，天天基金和东方财富优先，其次集思录、HaoETF、上交所和深交所，数据不全时继续找真实来源
2. [已开发] 修复bug：1/详情页的历史净值和溢价率列表好多数据但都没有 优化一下数据源 2/详情页顶部需要加安全区 有时候返回按钮回不去
3. [已开发] 全部的数据来源都优先使用天天基金/东方财富，新浪财经，其次集思录、HaoETF、上交所和深交所 非必要不用palmmicro 基金数据条目去除掉溢价率数据空的基金。
4. [已开发] 需求：新增用户埋点功能，在一个设备用户访问网页 就记录一个为客户访问，然后在项目下方新加一个后台管理系统，有登陆页和管理用户页面，可以查看有多少个用户 登陆页就一个输入密码 密码是53123，管理主页就先展示每天有几个新增用户 然后总计，先输出ui图然后发给我看 确认了再开发。
   - 需求目标：普通页面自动记录访问设备，后台 `/admin` 通过密码 `53123` 登录后查看今日新增用户、累计用户、累计访问、每日新增趋势和最近访问设备。
   - 数据口径：浏览器本地生成 deviceId，服务端按 deviceId 去重；每日新增按设备首次访问的上海日期统计，生产数据存储在服务器 shared 目录。
   - 验收结果：已按用户要求跳过 UI 图确认直接开发；接口、页面、测试、类型检查和构建均已验证通过。
5. [已开发] bug：数据来源优化，lof基金条目用https://palmmicro.com/woody/res/lofcn.php?sort=premium 用premium这里的数据 但仅仅参考首页表格的条目，其他的数据源展示用其他的数据源，数据源不展示premium qdii类型的数据就去除掉黄金类基金（溢价率高的离谱数据去掉 没有参考性）
   - 需求目标：LOF 类型只展示 Palmmicro 首页表格中的基金条目，但行情、净值和溢价展示仍使用东方财富、天天基金、新浪等其他真实来源；QDII 类型去除黄金/贵金属类基金。
   - 数据口径：Palmmicro 只作为 LOF 代码名单来源，不作为前端展示行情源；源站原始分类为 QDII/ETF 的 QDII-LOF，只要代码在 Palmmicro LOF 名单内，就归入 LOF 类型展示。
   - 验收结果：LOF 聚合接口 sourceProvider 为新浪 fallback，Palmmicro 展示行数为 0；QDII 聚合接口黄金类基金为 0。
6. [待开发] bug：首页和详情的数据再检查一下 看是不是有数据缺失 比如溢价率 净值 估值 这些数据是不是有缺失的，详情也是 估值 历史表中场内涨幅 成交额没有数据 需要检查
